You are implementing Task 1: Update Roster Types

## Task Description

### Task 1: Update Roster Types

**Files:**
- Modify: src/modules/roster/types.ts

- [ ] **Step 1: Define LeaveType union and update LeaveRecord**

Update src/modules/roster/types.ts to export LeaveType and use it in LeaveRecord.

```typescript
export type LeaveType = 'sick' | 'business' | 'vacation';

export interface LeaveRecord {
  id: string;
  employeeId: string;
  dateKey: string;
  leaveType: LeaveType;
  note?: string;
}
```

- [ ] **Step 2: Commit types update**

```bash
git add src/modules/roster/types.ts
git commit -m "chore: add LeaveType union and update LeaveRecord"
```

## Context

This is part of the Roster Module Enhancements. We are adding support for 'vacation' leave type. This is the first step which updates the core types used by the roster module.

## Before You Begin

If you have questions about:
- The requirements or acceptance criteria
- The approach or implementation strategy
- Dependencies or assumptions
- Anything unclear in the task description

**Ask them now.** Raise any concerns before starting work.

## Your Job

Once you're clear on requirements:
1. Implement exactly what the task specifies
2. Write tests (following TDD if task says to) - Note: This is a type-only change, so verify with tsc if possible.
3. Verify implementation works
4. Commit your work
5. Self-review (see below)
6. Report back

Work from: C:\Workplace\QSMS_project

**While you work:** If you encounter something unexpected or unclear, **ask questions**.
It's always OK to pause and clarify. Don't guess or make assumptions.

## Self-Review Checklist

- [ ] My implementation exactly matches the task description?
- [ ] I followed the code organization guidelines (small, focused files)?
- [ ] I wrote tests (if applicable) and verified they pass?
- [ ] My changes are committed with the requested message?
- [ ] My implementation is complete and correct?

## Reporting Back

When you finish, report back with:
- Status (DONE, DONE_WITH_CONCERNS, NEEDS_CONTEXT, BLOCKED)
- Summary of changes
- Git SHAs for your commits
- Any concerns or observations
